# Values ship reads. Format and keys: anderix/ship, docs/DESIGN.md.
#
# The package is Architecture: all — one file serves every architecture — and
# no workflow builds it yet: the .deb is built by hand and attached to the
# release by hand.
targets=apt
apt_package=slipcase-common
display_name=Slipcase Common
no_ci=true
